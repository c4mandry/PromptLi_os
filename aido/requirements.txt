# AIDO runtime dependencies (all MIT/Apache-2.0 licensed).
# Install with: pip install -r requirements.txt
# Development extras (pytest) are in setup.py: pip install -e ".[dev]"
llama-cpp-python>=0.3.2
playwright>=1.44.0
PyYAML>=6.0.1
requests>=2.31.0
